The service in this directory crashes at startup with a TypeError. Run
`python3 server.py` to see it. The config file `settings.conf` is correct and
must not be modified. Fix the bug at the appropriate layer so the server starts
and reports the correct effective timeout. Keep the public function names
unchanged.
