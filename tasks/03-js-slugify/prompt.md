Implement `slugify` in `slugify.mjs` so it fully matches the specification in
README.md. The current implementation handles the easy cases but fails several
of the specified edge cases. Do not add any dependencies; plain Node built-ins
only. You can try cases with `node -e "import('./slugify.mjs').then(m => console.log(m.slugify('...')))"`.
